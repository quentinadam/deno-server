name: CI
on:
  push:
    branches:
      - main

jobs:
  ci-publish:
    uses: quentinadam/deno-ci-workflow/.github/workflows/ci-publish.yml@v1
    with:
      skip-npm: true
    permissions:
      contents: write
      id-token: write
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
